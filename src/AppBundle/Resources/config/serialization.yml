AppBundle\Entity\Client:
    attributes:
        id:
            groups: ['client', 'location']
        name:
            groups: ['client', 'location']
        mdp:
            groups: ['client', 'location']
        lastname:
            groups: ['client', 'location']
        email:
            groups: ['client', 'location']
        birthdate:
            groups: ['client', 'location']  
        sex:
            groups: ['client', 'location']
        commandes:
            groups: ['client', 'location']
        basket_parent:
            groups: ['client', 'location']  
        locations:
            groups: ['client']


AppBundle\Entity\Location:
    attributes:
        id:
            groups: ['client', 'location']
        adress:
            groups: ['client', 'location']
        city:
            groups: ['client', 'location']
        codePostale:
            groups: ['client', 'location']
        tel:
            groups: ['client', 'location']
        client:
            groups: ['location']


AppBundle\Entity\Article:
    attributes:
        id:
            groups: ['article', 'categorie', 'client']
        name:
            groups: ['article', 'categorie', 'client']
        description:
            groups: ['article', 'categorie', 'client']
        price:
            groups: ['article', 'categorie', 'client']
        img:
            groups: ['article', 'categorie', 'client']
        marque:
            groups: ['article', 'categorie', 'client']
        categories:
            groups: ['article', 'categorie', 'client']
        basket:
            groups: ['article']  

AppBundle\Entity\Categorie:
    attributes:
        name:
            groups: ['categorie', 'article', 'client'] 

AppBundle\Entity\Basket:
    attributes:
        price:
            groups: ['client', 'basket'] 
        delivry:
            groups: ['client', 'basket'] 

